// import memoize from "memoize";
// import { TimelineRowExtra } from "~/features/tree-timeline/functions/createRow";
// import { SelectedLightIdsByGroupType } from "./useEditorStore";
// import memoizedSelectedIdsSelector from "./memoizedSelectedIdsSelector";

// export function selectedLightIdsSelector(
//   selectedLightIdsByGroup: SelectedLightIdsByGroupType,
//   rows: TimelineRowExtra[],
// ) {
//   // if (selectedLightId !== null) {
//   const { selectedGroupId } = memoizedSelectedIdsSelector(rows);
//   if (selectedGroupId !== undefined) {
//     const selectedLightIds = selectedLightIdsByGroup[selectedGroupId] ?? [];
//     return selectedLightIds;
//   }

//   return [];
// }

// const memoizedSelectedLightIdsSelector = memoize(selectedLightIdsSelector, {
//   cacheKey: JSON.stringify,
// });

// export default memoizedSelectedLightIdsSelector;
